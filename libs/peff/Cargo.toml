[package]
name = "peff"
version = "0.1.0"
authors = ["Terrence Cole <terrence.d.cole@gmail.com>"]

[dependencies]
log = "0.4"
bitflags = "1.0"
failure = "0.1"
packed_struct = { path = "../packed_struct/" }

[dev-dependencies]
omnilib = { path = "../omnilib" }
