[package]
name = "ot"
version = "0.1.0"
authors = ["Terrence Cole <terrence.d.cole@gmail.com>"]

[dependencies]
bitflags = "1.0"
failure = ">= 0.1.2"
entity = { path = "../entity/" }
sh = { path = "../sh" }

[dev-dependencies]
omnilib = { path = "../omnilib" }
